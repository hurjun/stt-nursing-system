# MediVoice runs fully client-side with synthetic data, so no secrets are required.
# These variables are placeholders for a future server-backed deployment.

# Speech-to-Text provider (the prototype defaults to the browser Web Speech API)
VITE_STT_PROVIDER=web-speech

# Default bedside interaction locale used by the AI speaker
VITE_DEFAULT_LOCALE=en-US
